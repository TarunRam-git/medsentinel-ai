"use client";
import { useState } from "react";
import { ArrowRight, Check, FlaskConical, LoaderCircle, ShieldCheck } from "lucide-react";
import { roles } from "@/lib/types";
export default function Login({demo}:{demo:boolean}){const [busy,setBusy]=useState(false);const [error,setError]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [role,setRole]=useState("admin");async function login(path:string,body:unknown){setBusy(true);setError("");try{const r=await fetch(`/api/auth/${path}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const result=await r.json();if(!r.ok)throw new Error(result.error);window.location.assign("/overview");}catch(e){setError((e as Error).message);setBusy(false);}}
return <main className="login-page"><section className="login-story"><div className="brand"><span className="brand-mark"><ShieldCheck size={23}/></span>MedSentinel <span className="brand-ai">AI</span></div><div className="login-story-content"><span className="eyebrow">CLINICAL INTEGRITY. SHARED CLARITY.</span><h1>A clearer view<br/>of safer care.</h1><p>Connect the evidence behind every alert.<br/>Keep your team—and your decisions—in sync.</p><div className="login-evidence"><div><span className="evidence-icon"><ShieldCheck size={24}/></span><div><b>One alert. The complete context.</b><small>Patient · Device · EHR · Security</small></div><span className="live-dot"/></div><div className="evidence-flow"><span>Observe</span><i/><span>Understand</span><i/><span>Review</span></div></div><div className="login-promises"><span><Check size={15}/> Evidence-linked insights</span><span><Check size={15}/> Human-reviewed responses</span></div></div><p className="login-foot">MedSentinel AI · Smart hospital research platform</p></section><section className="login-form-side"><div className="login-form-wrap"><span className="eyebrow">WELCOME TO YOUR WORKSPACE</span><h2>Sign in to MedSentinel</h2><p className="muted">A shared space for safer, more informed decisions.</p>{error&&<div className="error-message" role="alert">{error}</div>}<form onSubmit={e=>{e.preventDefault();void login("login",{email,password});}}><label>Email address<input type="email" required autoComplete="username" placeholder="you@hospital.org" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required autoComplete="current-password" placeholder="Enter your password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn primary full-width" disabled={busy}>{busy?<LoaderCircle className="spin" size={17}/>:<>Sign in securely <ArrowRight size={16}/></>}</button></form>{demo&&<div className="demo-login"><div className="demo-login-heading"><FlaskConical size={18}/><b>Explore the research demo</b></div><p>Use synthetic data to try the workspace. No account required.</p><label>Explore as<select value={role} onChange={e=>setRole(e.target.value)}>{roles.map(r=><option key={r} value={r}>{r[0].toUpperCase()+r.slice(1)}</option>)}</select></label><button className="btn full-width" disabled={busy} onClick={()=>login("demo",{role})}>Enter demo workspace <ArrowRight size={15}/></button></div>}<p className="login-disclaimer"><ShieldCheck size={15}/> Research prototype. Not for clinical diagnosis, treatment, or autonomous response.</p></div></section></main>;
}
